Currently access token isn't stored as secure or http only (on react side)?

Add likes to profile page

Add random article

add logo for logged in user by logout? and name? instead of logged in as x?

link comments in profile to the article page and specific comment. Update comment queries URL?

manage 404s better. Delete from the backend to minimise error messages on react

Use infinite scroll on comments.

when article image disappears, make comments full width

add infinite scroll to articles

create starred comments list
